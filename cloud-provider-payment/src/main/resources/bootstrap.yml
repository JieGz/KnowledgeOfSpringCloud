server:
  port: 8080

spring:
  application:
    name: knowledge-of-spring-cloud
  cloud:
    nacos:
      server-addr: http://nacos.itnaodong.com
      config:
        group: DEV_GROUP
        file-extension: yml
