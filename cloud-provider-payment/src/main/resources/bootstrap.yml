server:
  port: 8080

spring:
  application:
    name: knowledge-of-spring-cloud
  cloud:
    nacos:
      server-addr: http://nacos.itnaodong.com
      config:
        namespace: DEV_NACE_SPACE
        group: DEV_GROUP
        file-extension: yml
