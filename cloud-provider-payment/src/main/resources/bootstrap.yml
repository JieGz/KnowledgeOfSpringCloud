server:
  port: 8080

spring:
  application:
    name: knowledge-of-spring-cloud
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        namespace: NAME_SPACE_DEV
        group: DEV_GROUP
        file-extension: yml
